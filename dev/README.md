# Development and Utilities (`dev/`)

A utility folder mostly meant as a scratchpad for developers contributing specifically to the repo.

Includes logic for formatting synthetic data (`gen_synthetic_data.py`), dataset repackaging (`repackage_data_reference.py`), and project assets like logos.
